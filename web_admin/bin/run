#!/usr/bin/env bash

if [ "$RACK_ENV" == "production" ]; then
  mkdir -p tmp/pids
  pidfile="tmp/pids/thin.pid"
  [ -f "$pidfile" ] && kill -9 $(<"$pidfile") 2> /dev/null
  /usr/local/sbin/thin --rackup config.ru -p 4567 start
else
  rerun "thin --rackup config.ru -p 4567 start"
fi

