#!/bin/bash


# It worked! 

# Now, it is time for some post install tests.

# First, reboot the RPi nicely
ssh -oStrictHostKeyChecking=no pi@$rpi_hostname '/usr/bin/sudo /sbin/reboot'

sleep 30
sleep 30
sleep 30
sleep 40

# Test to see if the rpi is currently up. 
ping -c1 $rpi_hostname

