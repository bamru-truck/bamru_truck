#!/bin/bash

# Load environment variables
eval `./bin/ci/gen_env`

# Restart RPi
./powercycle/reset_arduino -t $switch_tty

# Wait for RPi to reboot
sleep 30
sleep 30
sleep 30

# Test to see if the rpi is currently up 
ping -c1 $rpi_hostname
